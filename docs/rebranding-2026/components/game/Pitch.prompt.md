Campo. Envolva com `AdBoard` nos quatro lados e ponha o `BenchList` ao lado.

```jsx
<Pitch watermark={club.crest} lines={[ataque, meio, defesa, [goleiro]]} />
```

O campo é retangular e alto; não achate. Ele precisa de `min-width:0` no pai para encolher.
