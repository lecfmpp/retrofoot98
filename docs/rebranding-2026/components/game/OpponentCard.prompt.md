Único bloco escuro da tela: o próximo jogo. O CTA de jogar mora aqui, não numa barra de ações solta.

```jsx
<OpponentCard opponent="Cianorte" rows={[…]}
  action={<Button variant="primary" pulse full>⚽ Jogar</Button>} />
```
