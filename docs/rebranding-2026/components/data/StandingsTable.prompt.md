Classificação. Combine com `Chip` para alternar entre Série D, Copa do Brasil, Libertadores e Sul-Americana — passe `animateKey` para a troca ganhar o fade-up.

```jsx
<StandingsTable rows={comp.rows} animateKey={comp.label} />
```
