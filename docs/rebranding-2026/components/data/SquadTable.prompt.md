Tabela do elenco. Sem linhas de grade: as linhas se separam por zebra. Números sempre em mono, alinhados à direita.

```jsx
<SquadTable players={squad} onSelect={setSelected} />
```

Não corte colunas para "dar respiro" — a densidade é característica do jogo.
