Card. Sem sombra (a sombra vive só no shell da tela).

```jsx
<Card grow><SectionLabel>CLASSIFICAÇÃO · SÉRIE D</SectionLabel>…</Card>
<Card tone="club" height={260}>…</Card>
```

Para alinhar as bases de duas colunas, dê `grow` ao último card da coluna mais curta.
